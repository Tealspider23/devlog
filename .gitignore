# ---- devlog data (NEVER commit: this is your personal activity log) ----
*.db
*.db-wal
*.db-shm
*.sqlite
api-token.txt

# Real local repo paths and any historical author emails belong here, not in
# appsettings.json - that file is public. ASP.NET Core layers this over the
# base config automatically; nothing needs updating in Program.cs to use it.
appsettings.local.json
appsettings.*.local.json

# ---- held back from the public repo for now ----
# These exist locally and still run; they are simply not published yet, because
# they carry real window titles, machine names and colleague names captured
# during development. To publish: sanitise those strings, drop these two lines,
# and re-add the test project to Devlog.slnx.
backend/tests/
backend/src/Devlog.Host/Seed/

# ---- .NET ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo
*.userprefs
.vs/
*.binlog

# ---- Node / frontend ----
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Rust / Tauri (arrives later) ----
target/
src-tauri/target/

# ---- Editors ----
.idea/
*.swp
.DS_Store

# ---- Logs ----
logs/
*.log

/** The loading placeholder, byte-identical across all five routes until now — most likely string to drift if left copy-pasted. */
export function Skeleton() {
  return <div className="animate-pulse rounded-[var(--radius-card)] border border-line bg-surface py-16" />
}
